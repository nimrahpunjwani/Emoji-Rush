# Emoji-Rush
This project implements intelligent navigation in a 2D grid using a goal-based agent. The environment contains free paths and obstacles. Search algorithms like Breadth-First Search (BFS) and A* are used to find optimal paths efficiently while avoiding blocked cells.
